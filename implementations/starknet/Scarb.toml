[package]
name = "epoch_permutation_starknet"
version = "0.1.0"
edition = "2024_07"
cairo-version = "2.16.0"

[dependencies]
starknet = "2.16.0"

[dev-dependencies]
snforge_std = "0.57.0"

[[target.starknet-contract]]
sierra = true
casm = true

[tool.scarb]
allow-prebuilt-plugins = ["snforge_std"]
